create table skill (
skill_name varchar(255),
best_skill varchar(255)
);

create table subject (
subject_name varchar(255),
best_subject varchar(255)
); 

create table manage (
genre_of_issues varchar(255),
major_issue varchar(255)
)